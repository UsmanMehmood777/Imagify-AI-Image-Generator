/** @type {import('tailwindcss').Config} */
export default {
  content: [
    "./index.html",
    "./src/**/*.{js,ts,jsx,tsx}",
  ],
  theme: {
    extend: {
      // If the GreatStack video adds custom fonts or colors later, 
      // you will paste them here inside the extend object!
    },
  },
  plugins: [],
}